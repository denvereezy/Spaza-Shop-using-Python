{% extends "layout.html" %}
{% block body %}

<table class="table table-striped custab">
    <thead>
        <tr>
            <th>Product</th>
            <th>Amount sold</th>
            <th class="text-center">Action</th>
        </tr>
    </thead>
            <tr>
                {% for entry in entries %}
                <td>{{ entry.Name}}</td>
                <td>{{ entry.Qty }}</td>

            </tr>
             {% endfor %}
{% endblock %}
    </table>
